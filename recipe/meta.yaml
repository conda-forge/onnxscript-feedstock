{% set name = "onnxscript" %}
{% set version = "0.5.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/onnxscript-{{ version }}.tar.gz
  sha256: 74a9c0aa52e1afd4e14a90349763de3670b4d22442f348a0bbd8a503ded2f431

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=70.0.0
    - pip
  run:
    - python >={{ python_min }}
    - ml_dtypes
    - numpy
    - onnx-ir >=0.1.10,<2
    - onnx >=1.16
    - packaging
    - typing_extensions >=4.10

test:
  imports:
    - onnxscript
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/microsoft/onnxscript
  summary: Naturally author ONNX functions and models using a subset of Python
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - apmorton
