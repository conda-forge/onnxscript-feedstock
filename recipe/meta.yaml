{% set name = "onnxscript" %}
{% set version = "0.5.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/onnxscript-{{ version }}.tar.gz
  sha256: 480d572451bc233ed7f742b5005cb0c899594b2fdc28e15167dab26f7fd777ad

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=70.0.0
    - pip
  run:
    - python >={{ python_min }}
    - ml_dtypes
    - numpy
    - onnx-ir >=0.1.12,<2
    - onnx >=1.16
    - packaging
    - typing_extensions >=4.10

test:
  imports:
    - onnxscript
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/microsoft/onnxscript
  summary: Naturally author ONNX functions and models using a subset of Python
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - apmorton
